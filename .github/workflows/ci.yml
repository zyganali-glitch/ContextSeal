name: ContextSeal CI

on:
  workflow_dispatch:
  push:
    branches: [main, "agent/**", "codex/**", "reconcile/**"]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        node-version: [20, 24]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: npm ci --ignore-scripts
      - name: Test fail-closed DataHub bootstrap gates
        run: npm run datahub:safety:test
      - run: npm run validate
      - name: Prove validation is idempotent
        run: git diff --exit-code

  dbt-proof:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: astral-sh/setup-uv@v6
      - run: npm ci --ignore-scripts
      - name: Reproduce real dbt proof
        run: npm run dbt:proof:ci
      - name: Validate fresh real dbt proof
        run: node scripts/run-dbt-proof.js --check --evidence-output .contextseal/ci/real-dbt-proof.json
      - name: Validate committed real dbt proof
        run: npm run dbt:proof:check
      - name: Upload exact-commit dbt proof
        uses: actions/upload-artifact@v4
        with:
          name: real-dbt-proof-${{ github.sha }}
          path: .contextseal/ci/real-dbt-proof.json
          if-no-files-found: error
      - name: Prove dbt CI leaves tracked files unchanged
        run: git diff --exit-code

  container-smoke:
    needs: [validate, dbt-proof]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Build production image
        run: docker build -t contextseal:${{ github.sha }} .
      - name: Start fixture-only container
        run: docker run --detach --name contextseal-ci --publish 4173:4173 contextseal:${{ github.sha }}
      - name: Verify container health and API behavior
        run: |
          curl --fail --retry 10 --retry-all-errors --retry-delay 1 http://127.0.0.1:4173/api/health --output health.json
          jq --exit-status '.status == "ok" and .mode == "fixture" and .mutationsEnabled == false and (.evidenceBoundary | startswith("FIXTURE:"))' health.json
          curl --fail --request POST --header 'content-type: application/json' --data '{}' http://127.0.0.1:4173/api/analyze --output analysis.json
          node scripts/assert-fixture-analysis.js analysis.json
      - name: Show container logs
        if: always()
        run: docker logs contextseal-ci
      - name: Stop container
        if: always()
        run: docker rm --force contextseal-ci

  submission:
    needs: [validate, dbt-proof, container-smoke]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: npm ci --ignore-scripts
      - name: Verify exact-final submission gate
        run: npm run submission:check
