# Branch Reconciliation Matrix

## Scope

- Common ancestor verified: `e738268d16a686080208c3559bc0276725941df6`
- Current `main` line inspected: `35d64550a5825fee55bbfdba61bf6be6a76b7fe4` -> `8b44ce1f6ac4c68ed4cb0c3fe8c4f7b25be355db`
- Hardened line inspected: `65bea060e0ed6260067b57838fb0f358d1ce3d0d`
- Working branch for reconciliation: `reconcile/hardened-final-head`

Status legend:

- `DONE`: selection made and reconciled at current HEAD.
- `RESTORE_HARDENED`: current `main` is missing a stronger hardened-line capability.
- `PRESERVE_CURRENT`: current `main` already owns the stronger implementation and must not regress.
- `DOCS_SYNC_PENDING`: implementation choice is known, but repository surfaces still need synchronized wording.

## Matrix

| Area | Current `main` behavior | Hardened-line behavior | Selected final behavior | Files involved | Tests / checks proving preservation | Status | Notes / evidence boundary |
|---|---|---|---|---|---|---|---|
| Branch graph and ancestry | Current repo contains AI, sandbox, PR bundle, UI polish, and living plan work | Hardened line diverged after the same ancestor with stronger DataHub, security, CI, and Skills work | Keep current `main` as the architectural base, port hardened capabilities selectively, and document every manual merge | `plans/PLAN_20260721_contextseal_hackathon_win.md`, `docs/BRANCH_RECONCILIATION_MATRIX.md` | `git status`; `git branch --all`; `git log --oneline --decorate --graph --all`; `git merge-base`; diff-stat review | `DONE` | No history rewrite on `main`; all reconciliation happens on `reconcile/hardened-final-head`. |
| CI workflows | Current `main` did not carry the restored final-head workflow contract for Node 20/24, Python safety, dispatchable CI, and idempotence proof | Hardened line preserves the stronger CI, Pages, and container-smoke shape | Restore hardened workflows, then update them for current scripts and final-head truth | `.github/workflows/ci.yml`, `.github/workflows/pages.yml`, `package.json`, `Dockerfile`, `compose.yaml` | Workflow review; local equivalents of `npm run validate`, smoke, Docker, and idempotence checks | `DONE` | Workflows, package scripts, dispatch triggers, and smoke/idempotence wiring are restored; exact-commit hosted execution still belongs to the final validation gate. |
| Server integration | Current server exposes fixture demo, AI enrichment, approval, live evidence capture, and PR-friendly API flow, but uses a simpler live-mode safety envelope | Hardened server adds operator-token auth, allowed-target enforcement, safe JSON parsing, supersede-aware state handling, write-back read-back verification, and stronger HTTP headers | Manual merge: preserve current AI entry points and judge flow while carrying hardened auth, state, and write-back verification guards | `src/server.js`, `src/store.js`, `src/datahub/writeback.js`, `src/datahub/live-context.js`, `src/ai/*` | `tests/server-integration.test.js`; `npm test`; `npm run smoke` | `DONE` | AI remains explanatory only; live mode is now guarded by operator auth, target allowlists, supersede checks, and durable read-back verification. |
| MCP client provenance | Current client handles basic HTTP/stdio sessions and `isError` tool failures | Hardened client adds stricter provenance, protocol handling, redaction, non-JSON rejection, and defensive diagnostics | Prefer hardened client, then keep any current-main compatibility needed by AI/demo scripts | `src/datahub/mcp-client.js`, `tests/mcp-client.test.js` | `tests/mcp-client.test.js`; live-pipeline tests | `DONE` | MCP provenance must not conflate runtime package version, protocol version, or server identity. |
| Live context normalization | Current live collection stores three raw MCP calls and leaves normalized impact to fixtures | Hardened line normalizes raw MCP evidence into a verified live context with schema completeness, exact paths, query binding, and raw hash binding | Adopt hardened normalization and keep the current fixture/live UI distinction | `src/datahub/live-context.js`, `src/core/workflow.js`, `src/server.js`, `docs/EVIDENCE_BOUNDARY.md` | `tests/live-pipeline.test.js`; `npm test`; fresh disposable-local recapture when available | `DONE` | Fixture graphs remain fixture. Final-head live proof still needs recapture, but the runtime now fails closed until hardened normalization passes. |
| Complete schema enumeration | Current `main` does not page schema fields or prove completeness | Hardened line validates `list_schema_fields` pagination, field completeness, collisions, and bounds | Restore hardened schema enumeration and reject incomplete pages | `src/datahub/live-context.js`, `tests/live-pipeline.test.js` | Targeted live-pipeline schema tests | `DONE` | Missing pages remain `FAIL` or `NOT_RUN`, never inferred success. |
| Lineage completeness | Current `main` stores one `get_lineage` response and does not prove completeness or truncation handling | Hardened line validates downstream discovery, exact-path retrieval, truncation rejection, and bounded hops | Restore hardened lineage completeness and exact-path checks | `src/datahub/live-context.js`, `src/core/workflow.js`, `tests/live-pipeline.test.js` | Live-pipeline lineage tests | `DONE` | No field-level lineage claim is permitted; only exact entity-level path coverage may be claimed. |
| Exact downstream path coverage | Current judge path uses fixture path reconstruction | Hardened line verifies per-target `get_lineage_paths_between` coverage | Keep fixture visualization for the public demo; add hardened live exact-path verification for final-head live proof | `src/datahub/live-context.js`, `public/app.js`, `docs/JUDGING_MAP.md`, `docs/EVIDENCE_BOUNDARY.md` | Live-pipeline tests; docs parity review | `DONE` | Runtime verification and public wording now agree that fixture visualization is separate from live exact-path proof; fresh final-head live recapture remains a later gate. |
| Query evidence honesty | Current AI contract already preserves zero-result live query language, but runtime live capture is shallow | Hardened line validates query result structure, target binding, truncation, and zero-result honesty | Merge hardened query validator with current AI-safe wording and docs | `src/datahub/live-context.js`, `src/ai/contracts.js`, `docs/EVIDENCE_BOUNDARY.md`, `tests/live-pipeline.test.js`, `tests/ai-contracts.test.js` | `npm test`; live-pipeline query tests | `DONE` | Runtime, tests, and submission-facing docs now preserve zero-result honesty while keeping final-head live recapture separate. |
| Write-back mutation envelope | Current write-back builds three bounded operations and stops on tool failure | Hardened line verifies policy allowlist, certified operation binding, sanitized receipts, and TOCTOU-safe execution | Carry hardened mutation validation forward while keeping the same bounded mutation list | `src/datahub/writeback.js`, `config/policy.json`, `tests/workflow.test.js`, `tests/server-integration.test.js` | Workflow tests; server-integration write-back tests | `DONE` | Mutations stay optional, token-gated, and impossible in fixture mode. |
| Durable read-back verification | Current `main` marks write-back complete but does not verify structured properties, description, and document binding afterward | Hardened line performs post-write read-back verification and fails if durable reads do not prove the mutation | Restore hardened read-back verification and expose its evidence state separately | `src/datahub/writeback.js`, `src/server.js`, `examples/outputs/live-datahub-writeback-evidence.json`, `tests/server-integration.test.js` | Server-integration tests; disposable-local live write-back recapture | `DONE` | Historical live proof remains useful, but final-head proof only counts after recapture. |
| Passport integrity | Current passport binds request/context/risk/artifacts and grounding rule identifiers, but lacks replay/stale/tamper/supersede verification | Hardened line adds policy binding, impact hash, live evidence binding, expiry validation, and verification helpers | Manual merge: keep current artifact grounding identifiers and add hardened verification/binding rules | `src/core/passport.js`, `src/core/workflow.js`, `tests/workflow.test.js` | Workflow tests plus hardened passport tests folded into integration suite | `DONE` | Passport remains the durable product hero; deterministic evidence stays authoritative. |
| Replay, stale, supersede, and store safety | Current store persists runs and events but lacks atomic write locks and supersede support | Hardened line adds locked atomic writes, expected-state checks, supersede flow, and safer file permissions | Restore hardened store as the persistence base | `src/store.js`, `tests/store.test.js`, `src/server.js` | `tests/store.test.js`; server-integration supersede tests | `DONE` | A stale or superseded run is now blocked rather than silently reusable. |
| Credential scanning | No credential-shaped key/value scanner exists on current `main` | Hardened line rejects credential-like keys/values and withholds secret-shaped diagnostics | Restore hardened credential scanning and thread it through live evidence, write-back, request, and approval validators | `src/security/credential-scan.js`, `tests/credential-scan.test.js`, `src/datahub/live-context.js`, `src/datahub/writeback.js`, `src/core/contracts.js` | `tests/credential-scan.test.js`; live-pipeline tests; server-integration tests | `DONE` | No secret may be echoed into artifacts, logs, API errors, approval records, or AI payloads. |
| Evidence validation | Current repo has an evidence manifest doc but no committed evidence validator script | Hardened line adds a stricter evidence validator and regression tests | Restore the hardened validator, then extend it for AI/sandbox/PR artifacts added on current `main` | `scripts/validate-evidence.js`, `tests/evidence-validator.test.js`, `docs/EVIDENCE_MANIFEST.md`, `examples/outputs/*` | `npm run evidence:check`; validator unit tests | `DONE` | Validator and regression tests are restored, and `npm run evidence:check` now isolates the remaining blocker to stale historical artifacts that lack the final five-tool, pinned-launcher, v2-passport, and durable-read-back proof shape. |
| DataHub Skills package | Current repo only carries `skills/contextseal-change-certification/SKILL.md` | Hardened line contains the more complete `skills/datahub-schema-change-certification/*` package and contribution docs | Canonicalize on the upstream PR #35 skill name, then document any compatibility bridge from the legacy local name | `skills/contextseal-change-certification/*`, `skills/datahub-schema-change-certification/*`, `docs/DATAHUB_SKILL_CONTRIBUTION.md`, `README.md` | Skill self-tests/evaluations; doc parity review | `DONE` | Canonical package naming, compatibility alias behavior, and PR #35 status wording are now synchronized across the repository. |
| AI adapter and contracts | Current `main` has the strongest AI layer: Ollama default, bounded schema, fallback states, visible UI, and saved artifacts | Hardened line predates this AI work | Preserve current AI architecture and only add any missing security clamps discovered during reconciliation | `src/ai/adapter.js`, `src/ai/contracts.js`, `tests/ai-adapter.test.js`, `tests/ai-contracts.test.js`, `public/*` | AI contract tests; adapter tests; demo generation | `PRESERVE_CURRENT` | AI status stays separate from deterministic evidence and never alters verdict or mutation authority. |
| Artifact generator and grounding manifest | Current `main` already adds `artifacts.grounding` and `ARTIFACT_MANIFEST.json` | Hardened line has stronger artifact/passport binding expectations but no current PR/demo surfaces | Preserve current manifest and enrich it where hardened proof needs extra bindings | `src/core/artifacts.js`, `src/core/passport.js`, `examples/outputs/generated/ARTIFACT_MANIFEST.json`, `tests/artifacts.test.js` | Artifact tests; `npm run demo:generate`; `npm run demo:check`; `npm run sandbox:generate`; `npm run sandbox:check` | `DONE` | Generated artifacts remain deterministic, reviewable, and explicitly not warehouse execution; committed fixture artifacts are now generated with stable timestamps and check-only parity commands. |
| Deterministic sandbox proof | Current `main` adds the deterministic manifest/hash conformance harness | Hardened line does not have the same current sandbox path | Preserve current sandbox and add any hardened validation coverage that can strengthen it without overclaiming | `scripts/run-generated-sandbox.py`, `tests/sandbox-conformance.test.js`, `examples/outputs/sandbox/*` | `npm run sandbox`; sandbox tests | `PRESERVE_CURRENT` | This remains deterministic local conformance unless local dbt/DuckDB proof is safely added. |
| PR handoff and optional draft PR | Current `main` has the strongest PR flow: offline packet generator, dry-run draft creation, and committed artifacts | Hardened line predates the current PR packet work | Preserve current PR flow, then harden repo/base-branch validation and integrate final-head evidence links | `scripts/build-pr-bundle.js`, `scripts/create-draft-pr.js`, `docs/PR_REVIEW_PACKET.md`, `examples/outputs/pr/*` | `npm run pr:bundle`; `npm run pr:draft -- --dry-run`; integration checks | `PRESERVE_CURRENT` | Live draft PR creation remains explicit, token-gated, and never auto-merge. |
| UI and demo path | Current `main` has the stronger hero, inheritance strip, AI panel, and compressed 100-second flow | Hardened line has an earlier, less polished UI | Preserve current UI and only update copy where reconciliation changes proof truth | `public/index.html`, `public/app.js`, `public/styles.css`, `public/demo-data.json`, `docs/DEMO_SCRIPT.md`, `docs/UI_REVIEW.md` | Browser review; `npm run demo`; `npm run validate` | `PRESERVE_CURRENT` | No screen may imply live proof, AI proof, or warehouse execution that did not occur. |
| README, Devpost, judge docs, and evidence docs | Current `main` improved README top-fold, visual direction, demo script, and PR packet docs, but still needs final parity and upstream PR truth fixes | Hardened line has fuller evidence, live setup, and pre-submission docs, but weaker current AI/demo framing | Merge both: keep current story frame, restore stronger evidence/live setup docs, and align all submission surfaces to the reconciled truth | `README.md`, `README.tr.md`, `docs/DEVPOST_SUBMISSION.md`, `docs/JUDGE_TEST_PATH.md`, `docs/JUDGING_MAP.md`, `docs/EVIDENCE_BOUNDARY.md`, `docs/EVIDENCE_MANIFEST.md`, `docs/LIVE_DATAHUB_SETUP.md`, `docs/COMPETITION_REQUIREMENT_MATRIX.md`, `docs/CLAIM_AUDIT.md`, `docs/tr/*` | Cross-surface parity review; `npm run evidence:check`; final plan review | `DONE` | Public docs now make historical proof versus final-head proof and upstream PR truth explicit; the remaining blocker is fresh live recapture and exact-head validation, not wording drift. |

## Immediate next closures

1. Recapture final-head disposable-local live evidence after seeding the synthetic DataHub graph with the restored fail-closed helpers.
2. Run the full reconciled validation suite, exact-head container smoke, and hosted/manual GitHub gates before deciding whether submission phases can resume.
3. Close `W-17L` with one final parity review after `W-17J` and `W-17K` either pass or remain explicitly blocked.