# Devpost media kit

These four sanitized PNG files are ready to upload to Devpost. Each is exactly `1500 × 1000` pixels (`3:2`) and below 5 MB, matching Devpost's [project thumbnail guidance](https://help.devpost.com/article/126-know-your-submission-steps).

They were captured from the real local ContextSeal application running in `fixture` mode. They are product screenshots with synthetic context, **not** live DataHub evidence. No browser chrome, workstation path, token, environment file, private tenant, or customer data is visible.

| File | Devpost use | Copy-ready caption |
| --- | --- | --- |
| [`contextseal-thumbnail.png`](contextseal-thumbnail.png) | Project thumbnail / first image | **Ship the change. Keep the proof.** ContextSeal turns a destructive schema request into a lineage-aware, human-gated migration plan. Local fixture execution with synthetic context. |
| [`contextseal-analysis.png`](contextseal-analysis.png) | Gallery image 1 | **Graph-grounded impact.** The deterministic fixture blocks the direct change at risk 80, traces five downstream assets, and proposes an expand–migrate–contract alternative. |
| [`contextseal-artifacts.png`](contextseal-artifacts.png) | Gallery image 2 | **Reviewable work, not advice.** ContextSeal explains every risk contribution and generates a dbt model, schema tests, rollback, and impacted-owner brief before approval. |
| [`contextseal-passport.png`](contextseal-passport.png) | Gallery image 3 | **Human-gated proof.** Approval binds the exact fixture context and artifact manifest into a change passport while warehouse SQL and DataHub write-back remain visibly `NOT_RUN`. |

Recommended upload order: thumbnail, analysis, artifacts, passport. Do not crop away the fixture boundary in the thumbnail or relabel any image as production/live proof.
